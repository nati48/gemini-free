# Your custom API key - anyone calling the server must send this
# Example: Authorization: Bearer my-super-secret-key
GEMINI_FREE_API_KEY=change-me-to-a-long-random-string

# Server config
HOST=0.0.0.0
PORT=8787

# Path to cookies file (created by `python -m gemini_free.auth`)
COOKIES_FILE=cookies.json
